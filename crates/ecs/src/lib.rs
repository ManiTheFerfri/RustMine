//! Entity-component-system for game entities.
//!
//! Uses `hecs` as the underlying ECS library. Components cover players,
//! mobs, items, projectiles, and other game objects.

/// Re-export of `hecs` for consumers.
pub use hecs;
